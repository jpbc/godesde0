package main

import "github.com/jpbc/godesde0/ciclos"

//"runtime"

//"github.com/jpbc/godesde0/ciclos"

func main() {
	/*
			estado, texto := variables.ConviertoaTexto(1234)
			fmt.Println(estado)
			fmt.Println(texto)

			if os := runtime.GOOS; os == "linux." || os == "OS X." {
				fmt.Println("Esto no es Windows es", os)
			} else {
				fmt.Println("Esto es Windows")

			}
			switch os := runtime.GOOS; os {
			case "linux":
				fmt.Println("Esto es Linux")
			case "darwin":
				fmt.Println("Esto es Darwin")
			default:
				fmt.Printf("%s \n", os)
			}
			numero, cadena := ejercicios.DevuelveIntString("58")
			fmt.Println(numero)
			fmt.Println(cadena)

		teclado.IngresoNumeros()
	*/
	ciclos.Ciclo()
}
